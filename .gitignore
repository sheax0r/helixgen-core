.venv-poc/
captures/
__pycache__/
*.pyc

# Captured device _sbepgsm reference presets (round-trip fixtures; not committed)
tests/fixtures/device_content/

# Captured .hss setlist-bundle exports (real-device fixtures; not committed)
tests/fixtures/hss/

# local dev venv + macOS cruft (never commit)
.venv/
.DS_Store

# paid commercial IR packs — never commit (user rule; see irs/_catalog for the shareable index)
irs/
